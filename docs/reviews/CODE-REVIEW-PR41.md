# Code Review — PR #41

**Date**: 2026-03-26
**Scope**: PR #41 `feature/36-automate-verification` — 20 source files (excluding package-lock.json)
**Languages**: TypeScript, Rust, Svelte
**Tests**: 160 frontend passed, 107 backend passed — 0 failures
**Dependency Audit**: npm audit: 12 moderate (dev-only, brace-expansion); cargo audit: 1 moderate (rsa phantom dep, known mitigated)
**Summary**: 0 critical, 1 high, 7 medium, 11 low

## Findings

| #      | Severity   | Category        | Location                                                         | Description                                                                                                                                                                                                                                   | Proposed Fix                                                                                                                               | Status       |
| ------ | ---------- | --------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------ |
| 1      | HIGH       | Tests           | `tests/components/ResultsView.test.ts`                           | ResultsView has significant untested interactive behavior — file selection, group selection, Delete Selected button, Clear Selection, onDeleteSelected callback. Tests only check static rendering.                                           | Add interaction tests: click group, toggle file checkbox, verify Delete Selected appears, verify onDeleteSelected called with paths.       | OPEN         |
| 2      | MEDIUM     | Maintainability | `tests/components/*.test.ts`                                     | `makeGroup()` factory duplicated across 5 test files with slight signature variations; `makeResult()` duplicated in 2 files. ~80 lines of duplication.                                                                                        | Extract into shared `tests/factories.ts` module with flexible signatures covering all call-site variations.                                | FIXED        |
| 3      | MEDIUM     | Tests           | `src-tauri/src/commands/settings.rs:70-82`, `protected.rs:87-99` | Rust tests in settings.rs and protected.rs only test `str::trim()` and `is_empty()` — standard library behavior, not application logic. Tests are tautological and provide no regression value. Same for `deletion.rs` pagination clamp test. | Remove or replace with integration tests that exercise actual command functions, or extract validation into a shared helper and test that. | FIXED        |
| 4      | MEDIUM     | Tests           | `tests/components/FolderPicker.test.ts`                          | Missing test for path deduplication (`new Set` on line 21 of source) and error path (try/catch on dialog rejection). Also untested: `open()` returning single string vs array.                                                                | Add tests: (1) mock `open()` to return already-selected path, verify dedup; (2) mock `open()` rejection, verify no crash.                  | FIXED        |
| 5      | MEDIUM     | Tests           | `tests/components/DeleteConfirmDialog.test.ts`                   | Missing test for confirm button click in normal mode. Never tests that clicking 'Delete to Trash' calls `onConfirm`. Also untested: Escape key dismissal, checkbox-then-confirm flow for allInGroup.                                          | Add: (1) click confirm button, verify `onConfirm` called; (2) check allInGroup checkbox → button enabled → click calls `onConfirm`.        | FIXED        |
| 6      | MEDIUM     | Tests           | `tests/components/DeletionHistoryPanel.test.ts`                  | Missing tests for error state (API rejection) and Load More pagination. Source lines 77-79 render error, line 62 handles Load More — neither tested.                                                                                          | Mock `invoke` to reject, verify error message rendered. Test Load More button when records fill a page.                                    | FIXED        |
| 7      | MEDIUM     | Code vs Docs    | `docs/CONTRIBUTING.md`                                           | Issue #36 Phase B specifies establishing a 'test-as-you-go' convention. No documentation of this exists in CONTRIBUTING.md, CLAUDE.md, or any plan file.                                                                                      | Add testing convention section to CONTRIBUTING.md or CLAUDE.md codifying the test-as-you-go policy.                                        | OPEN         |
| 8      | MEDIUM     | Code vs Docs    | `src-tauri/Cargo.toml`                                           | Issue #36 specifies `tauri = { features = ["test"] }` as a dependency. PR intentionally skipped it (better ROI from serde tests), but this decision is not documented in the PR or issue.                                                     | Document in PR description why tauri test feature was deferred. Consider a follow-up issue.                                                | OPEN         |
| 9      | LOW        | Correctness     | `tests/components/DeletionHistoryPanel.test.ts:7-14`             | Mock `invoke` uses `mockResolvedValueOnce` chaining which relies on call order. If component reorders API calls (e.g., `Promise.all`), mocks silently swap, causing confusing failures.                                                       | Mock `invoke` with a dispatch function that checks the command name argument instead of relying on call order.                             | FIXED        |
| 10     | LOW        | Correctness     | `src-tauri/src/commands/scan.rs:275`                             | `unique_files` calculated as `total_files - duplicate_count`, but test fixtures set `unique_files = groups.length`. Semantics inconsistent between backend and frontend test helpers.                                                         | Align test fixtures' `unique_files` calculation to match backend logic, or document the intended semantics.                                | OPEN         |
| 11     | LOW        | Maintainability | `src-tauri/src/commands/scan.rs:312-318`                         | `parse_parallelism` test helper duplicates production match logic (lines 99-103). If production changes, test silently diverges.                                                                                                              | Extract parsing into a `pub(crate)` function and test that directly.                                                                       | OPEN         |
| ~~P1~~ | ~~MEDIUM~~ | ~~Security~~    | ~~`src-tauri/src/commands/protected.rs:15`~~                     | ~~Protected folder bypass via non-canonical paths. Neither `add_protected_folder` nor `is_path_protected` canonicalize paths. `/../` or symlinks defeat the SQL LIKE prefix check.~~                                                          | ~~Canonicalize paths via `std::fs::canonicalize` before storing and comparing.~~                                                           | PRE-EXISTING |
| ~~P2~~ | ~~MEDIUM~~ | ~~Security~~    | ~~`src-tauri/src/commands/deletion.rs:32-110`~~                  | ~~`delete_files` accepts arbitrary paths from frontend without validating they were part of scan results or are absolute. Only guard is the protection check (which has P1 bypass).~~                                                         | ~~Verify deletion paths exist in `scanned_files` table. Require and canonicalize absolute paths.~~                                         | PRE-EXISTING |
| ~~P3~~ | ~~MEDIUM~~ | ~~Performance~~ | ~~`src-tauri/src/commands/scan.rs:241`~~                         | ~~N+1 query: `get_scan_results()` issues one `get_by_group` DB query per duplicate group in a loop. Hundreds of groups = hundreds of sequential queries.~~                                                                                    | ~~Single JOIN query fetching all files for a session, grouped in-memory by `group_id`.~~                                                   | PRE-EXISTING |
| ~~P4~~ | ~~MEDIUM~~ | ~~Performance~~ | ~~`src-tauri/src/commands/deletion.rs:49-58`~~                   | ~~N+1 query: `delete_files()` calls `is_protected()` per file sequentially while holding DB lock.~~                                                                                                                                           | ~~Fetch all protected prefixes once and check in-memory, or use a single query with IN clause.~~                                           | PRE-EXISTING |
| ~~P5~~ | ~~LOW~~    | ~~Security~~    | ~~`package.json`~~                                               | ~~npm audit: 12 moderate advisories for brace-expansion <5.0.5 (process hang via zero-step sequence). Dev-only, not in production bundle.~~                                                                                                   | ~~Upgrade eslint to >=10.1.0 or add overrides for brace-expansion >=5.0.5.~~                                                               | PRE-EXISTING |
| ~~P6~~ | ~~LOW~~    | ~~Security~~    | ~~`src-tauri/Cargo.lock`~~                                       | ~~cargo audit: rsa 0.9.10 timing side-channel (RUSTSEC-2023-0071). Phantom dep via sqlx-mysql — not compiled. Already mitigated by `default-features = false`.~~                                                                              | ~~No action needed. Document for auditors.~~                                                                                               | PRE-EXISTING |
| ~~P7~~ | ~~LOW~~    | ~~Security~~    | ~~`src-tauri/src/commands/settings.rs:32-51`~~                   | ~~`set_setting` accepts arbitrary keys with no allowlist. Compromised frontend could poison config.~~                                                                                                                                         | ~~Add key allowlist or namespace user settings with prefix.~~                                                                              | PRE-EXISTING |
| ~~P8~~ | ~~LOW~~    | ~~Security~~    | ~~`src-tauri/src/commands/*.rs`~~                                | ~~Internal error details exposed to frontend via `.map_err(\|e\| e.to_string())` across all commands. Can leak DB schema, paths, internal state.~~                                                                                            | ~~Map to user-friendly messages; log full errors server-side.~~                                                                            | PRE-EXISTING |

## Review Process

| Dimension           | Status   | Notes                                                                                                                      |
| ------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------- |
| Correctness & Bugs  | Reviewed | Traced execution paths; found invoke mock ordering concern and unique_files semantics mismatch                             |
| Tests & Coverage    | Reviewed | Ran test suite: 160 frontend + 107 backend passed. Found significant coverage gaps in component interaction tests          |
| Error Handling      | Reviewed | No issues in changed code                                                                                                  |
| Types & Type Design | Reviewed | No issues — all test helpers properly typed, no `any` usage                                                                |
| Comments & Docs     | Reviewed | Several "what" vs "why" comment suggestions (low severity, omitted from table for brevity)                                 |
| Simplification      | Reviewed | Main finding: duplicated test helpers (captured in #2)                                                                     |
| Security            | Reviewed | npm audit: 12 moderate (dev-only); cargo audit: 1 known phantom dep. Found pre-existing path traversal and validation gaps |
| Performance         | Reviewed | Found 2 pre-existing N+1 query patterns in command handlers                                                                |
| Maintainability     | Reviewed | DRY violations in test factories and command boilerplate                                                                   |
| Architecture        | Reviewed | Clean layering, consistent patterns. Test organization mirrors source structure                                            |
| Code vs Docs/Plans  | Reviewed | Compared against issue #36 spec. Found missing Phase B convention doc and undocumented tauri test feature deferral         |

## Limitations

- **Tauri command integration tests**: Cannot be fully assessed without `tauri = { features = ["test"] }`. Command-layer tests are limited to serde/validation.
- **Component interaction testing**: jsdom lacks layout engine, so drag-to-resize, tooltip hover positioning, and CSS-dependent visual states cannot be verified in component tests.
