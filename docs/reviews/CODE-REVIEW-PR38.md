# Code Review — PR #38

**Date**: 2026-03-24
**Scope**: PR #38 (`feature/fix-v1-v4-dal-bypass` vs `main`) — 3 files
**Languages**: Rust, Markdown
**Tests**: 88 Rust passed, 0 failed; 85 frontend passed, 0 failed
**Dependency Audit**: npm audit: 0 vulnerabilities; cargo-audit: not installed
**Summary**: 0 critical, 3 high, 8 medium, 6 low

## Findings

| #   | Severity | Category       | Location                          | Description                                                                                                                                                                                                                                       | Proposed Fix                                                                                                                                       | Scope                                                      |
| --- | -------- | -------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| 1   | HIGH     | Tests          | `queries.rs:369`                  | No test exercises the `ON CONFLICT(hash) DO UPDATE` path for `duplicate_groups::create`. The PR's core behavioral change is entirely untested.                                                                                                    | Add a test that calls `create` twice with the same hash, asserts row count is 1 and values updated.                                                | PR                                                         |
| 2   | HIGH     | Tests          | `queries.rs:491`                  | No test exercises the `ON CONFLICT(path) DO UPDATE` path for `scanned_files::insert`. The upsert behavior has zero direct test coverage.                                                                                                          | Add a test that inserts the same path twice, asserts row count is 1 and second values are reflected.                                               | PR                                                         |
| 3   | HIGH     | Error Handling | `scan.rs:253`                     | File insert errors are logged with `warn!` but `failed_groups` is never incremented. If all file inserts fail but group inserts succeed, the scan reports success with empty groups.                                                              | Track file-level failures; treat a group with zero files as a failed group.                                                                        | Pre-existing (tracked in #39)                              |
| 4   | MEDIUM   | Error Handling | `queries.rs:388-396`              | `ON CONFLICT(hash) DO UPDATE` means `failed_groups` can now only increment on true DB errors, never on hash conflicts. The all-groups-failed guard is nearly impossible to trigger under upsert semantics.                                        | Document that `failed_groups` now only catches DB errors. Consider failing if _any_ group insert errors since upserts should never fail normally.  | PR                                                         |
| 5   | MEDIUM   | Correctness    | `queries.rs:388-406`              | `ON CONFLICT(hash) DO UPDATE SET scan_session_id = excluded.scan_session_id` re-parents an existing group to a new session. Old `scanned_files` rows from the prior session still reference the group, creating cross-session data contamination. | Either delete old groups before re-scanning or don't update `scan_session_id` on conflict.                                                         | PR (theoretical — workflow doesn't trigger this currently) |
| 6   | MEDIUM   | Tests          | `queries.rs:377`                  | The generic `SqliteExecutor<'e>` bound is not tested with a transaction executor. All production call sites pass `&mut *tx` but no test mirrors this path.                                                                                        | Add a test that begins a transaction, calls `create`/`insert` via `&mut *tx`, commits, and verifies.                                               | PR                                                         |
| 7   | MEDIUM   | Comments       | `docs/reviews/CODE-REVIEW.md:8`   | Summary says "9 high (8 fixed, 1 open)" but all 9 highs (including V1) are now FIXED. The tally is stale.                                                                                                                                         | Update to "9 high (9 fixed, 0 open)".                                                                                                              | PR                                                         |
| 8   | MEDIUM   | Comments       | `docs/reviews/CODE-REVIEW.md:149` | Verification summary references "(V5)" but no V5 finding exists in the findings table. Dangling reference.                                                                                                                                        | Either add a V5 row or remove the "(V5)" label.                                                                                                    | PR                                                         |
| 9   | MEDIUM   | Performance    | `queries.rs:505-531`              | `scanned_files::insert` uses `RETURNING id` + `fetch_one`, but the caller discards the returned id. The `RETURNING` clause adds overhead on every insert in the inner loop.                                                                       | Add a separate `insert_no_return` variant using `execute` for call sites that don't need the id, or accept the minor overhead for API consistency. | PR                                                         |
| 10  | MEDIUM   | Simplification | `scan.rs:207`                     | Transaction started via verbose `sqlx::pool::Pool::begin(db_guard.pool())` instead of idiomatic `db_guard.pool().begin()`. Also bypasses the DAL for transaction lifecycle.                                                                       | Use `db_guard.pool().begin().await?` or add a `Database::begin_transaction()` method.                                                              | Pre-existing                                               |
| 11  | MEDIUM   | Architecture   | `scan.rs:207`                     | Service layer directly calls sqlx API to open transactions, bypassing the DAL. The PR moved SQL into `queries.rs` but left transaction management in the service layer — incomplete fix.                                                          | Add a `begin_transaction()` method to `Database` or a DAL-level `persist_scan_results` function.                                                   | Pre-existing (partially addressed by PR)                   |
| 12  | LOW      | Types          | `scan.rs:231`                     | `group.files.len() as i32` truncates silently if count exceeds `i32::MAX`. Suppressed by `#[allow]`.                                                                                                                                              | Use `i32::try_from(...).unwrap_or(i32::MAX)`.                                                                                                      | Pre-existing pattern                                       |
| 13  | LOW      | Comments       | `queries.rs:4`                    | Module NOTE lists `deletions::{record, get_history, get_count}` but actual module is `deletion_history`. Wrong name.                                                                                                                              | Change to `deletion_history::{record, get_history, get_count}`.                                                                                    | Pre-existing                                               |
| 14  | LOW      | Docs/Plans     | `docs/reviews/CODE-REVIEW.md:19`  | V1 location still points to `scan.rs:226-263` which now contains the corrected DAL calls, not the bug.                                                                                                                                            | Append "(fixed)" or update location to `queries.rs` where the fix lives.                                                                           | PR                                                         |
| 15  | LOW      | Security       | `queries.rs:759`                  | `cleanup_missing_files` builds dynamic SQL via `format!()` — only `?` placeholders are interpolated (not user data), but the pattern is fragile and has no upper bound on placeholder count.                                                      | Use `QueryBuilder` API or batch in chunks of 500. Add explanatory comment.                                                                         | Pre-existing                                               |
| 16  | LOW      | Simplification | `queries.rs:206-270`              | `get_latest` and `get_paused` duplicate identical 20-line row-to-struct mapping closures.                                                                                                                                                         | Extract `fn row_to_session(row: ScanSessionRow) -> ScanSession`.                                                                                   | Pre-existing                                               |
| 17  | LOW      | Performance    | `scan.rs:242`                     | `file.path.display().to_string()` allocates a new String per file in the inner loop.                                                                                                                                                              | Convert path to String once before the loop.                                                                                                       | Pre-existing                                               |

## Review Process

| Dimension           | Status   | Notes                                                                                                     |
| ------------------- | -------- | --------------------------------------------------------------------------------------------------------- |
| Correctness & Bugs  | Reviewed | Traced all execution paths; found cross-session contamination risk and stats inflation on partial failure |
| Tests & Coverage    | Reviewed | Ran test suite: 88 Rust + 85 frontend passed. Identified missing upsert and transaction-path tests        |
| Error Handling      | Reviewed | Found file-level failure gap (#39) and upsert impact on failure guard semantics                           |
| Types & Type Design | Reviewed | Generic `SqliteExecutor` bound is correct; cast truncation is theoretical                                 |
| Comments & Docs     | Reviewed | Found stale tally in docs/reviews/CODE-REVIEW.md summary and dangling V5 reference                        |
| Simplification      | Reviewed | Verbose transaction init; duplicated row-mapping closures                                                 |
| Security            | Reviewed | npm audit: clean. All queries use parameterized bindings. One `format!()` pattern noted                   |
| Performance         | Reviewed | `RETURNING id` overhead on discarded results; per-file path allocation                                    |
| Maintainability     | Reviewed | Transaction lifecycle still in service layer; PR is net positive for SQL centralization                   |
| Architecture        | Reviewed | Layer violation partially fixed — SQL moved to DAL but transaction management remains in service          |
| Code vs Docs/Plans  | Reviewed | V1/V4 FIXED markings are accurate; stale location reference noted                                         |

## Limitations

- `cargo-audit` not installed — could not run dependency vulnerability scan for Rust crates
- GitHub issue #37 returned 404 from one agent (likely auth scope) — verified against issue body cached in session instead
