# Code Review

**Date**: 2026-03-21
**Scope**: Full repo (64 source files)
**Languages**: Rust, TypeScript, Svelte 5
**Tests**: 85 frontend passed (Vitest), 68 backend passed (cargo test)
**Dependency Audit**: npm audit — high-severity vulnerability in `devalue` (transitive), moderate in `ajv`, `vite`, `vitest`; cargo audit — not installed
**Summary**: 0 critical, 8 high, 38 medium, 32 low

## Findings

| #   | Severity | Category        | Location                                              | Description                                                                                                                                                                                   | Proposed Fix                                                                                                           |
| --- | -------- | --------------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 1   | HIGH     | Architecture    | `src-tauri/src/commands/scan.rs:186-208`              | `cancel_scan` directly updates DB session status, while `ScanService::run()` also updates it on failure/completion. Dual ownership can cause race conditions (Cancelled vs Failed/Completed). | Delegate cancellation DB updates to `ScanService` — let `run()` handle the final status after detecting cancellation.  |
| 2   | HIGH     | Correctness     | `src-tauri/src/commands/scan.rs:273-289`              | DB `i64` fields (`file_size`, `wasted_space`) cast to `u64` with `as u64`. Negative values (from overflow during storage) wrap to huge incorrect values.                                      | Use `.max(0) as u64` or `u64::try_from(value).unwrap_or(0)` for all i64-to-u64 DB casts.                               |
| 3   | HIGH     | Error Handling  | `src-tauri/src/services/scan.rs:137`                  | `walker_handle.join().unwrap_or_default()` silently swallows thread panics. Walker panic → scan continues with zeroed stats, no error reported.                                               | Match on join result: if Err (panic), log payload and emit scan error event.                                           |
| 4   | HIGH     | Error Handling  | `src-tauri/src/services/scan.rs:209-241`              | Phase 3 DB persistence logs warnings for individual insert failures but continues. If all writes fail (disk full, DB locked), scan reports success with zero persisted results.               | Track failed write count. If all/most group inserts fail, emit scan error instead of completion.                       |
| 5   | HIGH     | Performance     | `src-tauri/src/scanner/detector.rs:222-253`           | Partial and full hashing are single-threaded. For thousands of size-matched candidates on SSDs, no cross-file parallelism is used. `full_hash_parallel` exists but is never called.           | Use `rayon::par_iter` to hash multiple files concurrently in both `group_by_partial_hash` and `verify_with_full_hash`. |
| 6   | HIGH     | Performance     | `src-tauri/src/scanner/hasher.rs:72-84`               | `partial_hash` allocates two new 4KB `Vec<u8>` on every call. Thousands of calls → thousands of short-lived heap allocations. The `FileHasher` already has a reusable 64KB `buffer` field.    | Reuse `self.buffer` instead of allocating fresh vectors on each call.                                                  |
| 7   | HIGH     | Performance     | `src-tauri/src/services/scan.rs:183-217`              | DB persistence inserts each group and file individually while holding the mutex. Hundreds of groups → thousands of sequential SQLite round-trips under a single lock.                         | Batch inserts using a single transaction (BEGIN/COMMIT) or multi-row INSERT statements.                                |
| 8   | HIGH     | Security        | `npm audit`                                           | npm audit reports high-severity vulnerability in `devalue` (transitive) and moderate in `ajv` (ReDoS), `vite`, `vitest`. Dev dependencies but `vite` affects dev server.                      | Update `vitest` to v4.x and `vite` to latest patched version. Run `npm audit fix`.                                     |
| 9   | MEDIUM   | Architecture    | `src-tauri/src/commands/scan.rs:268-299`              | `get_scan_results` manually maps DB rows to domain types in the command layer, bypassing the service layer. All other scan commands delegate to `ScanService`.                                | Create `ScanService::get_results(db)` method; command handler calls it.                                                |
| 10  | MEDIUM   | Architecture    | `src-tauri/src/commands/deletion.rs:48-59`            | Protected-path check iterates files and queries DB directly from command layer — business logic in the handler.                                                                               | Move protected-path validation into `DeletionService`.                                                                 |
| 11  | MEDIUM   | Architecture    | `src-tauri/src/services/deletion.rs:4`                | Imports `crate::scanner::hasher::HashError` directly from internal submodule instead of the public re-export `crate::scanner::HashError`.                                                     | Change to `use crate::scanner::HashError`.                                                                             |
| 12  | MEDIUM   | Architecture    | `src/App.svelte:140-180`                              | Deletion flow is inlined in root component (event listening, API calls, state transitions). Scan flow correctly uses `scanStore`/`ScanService`, but deletion has no equivalent.               | Create a `deletionStore` analogous to `scanStore` for deletion orchestration.                                          |
| 13  | MEDIUM   | Comments        | `src-tauri/src/db/models.rs:4-7`                      | Module NOTE references 'Phase 4/5' as future work, but all listed features are implemented. Blanket `#![allow(dead_code)]` suppresses warnings for items that are no longer dead.             | Remove outdated NOTE and blanket allow. Apply targeted allows only to genuinely unused items.                          |
| 14  | MEDIUM   | Comments        | `src-tauri/src/db/queries.rs:4-5`                     | NOTE says functions are 'used in future phases' but they're actively called. Blanket `#![allow(dead_code)]` hides genuinely unused items.                                                     | Remove outdated NOTE. Audit which functions are unused; apply targeted allows only to those.                           |
| 15  | MEDIUM   | Comments        | `src-tauri/src/scanner/types.rs:3-6`                  | NOTE says `thread_count()` is for future parallel hashing and error variants are reserved — both are already used.                                                                            | Remove outdated NOTE. Replace blanket `#![allow(dead_code)]` with targeted allows.                                     |
| 16  | MEDIUM   | Comments        | `src-tauri/src/scanner/mod.rs:3`                      | `#![allow(unused_imports)]` justified as 'used in future phases' but `HashError` is actively used (via a different import path). Blanket allow hides genuinely unused re-exports.             | Remove blanket allow. Fix deletion.rs to use the re-export path.                                                       |
| 17  | MEDIUM   | Comments        | `src-tauri/src/scanner/walker.rs:2-6`                 | NOTE references `get_progress()` but actual method is `progress()`. Design note about `create_walker()` belongs on the method, not module header.                                             | Fix method name in NOTE. Move design notes to doc comments on relevant methods.                                        |
| 18  | MEDIUM   | Comments        | `src/lib/types.ts:59`                                 | `ScanPhaseEvent` defines phases (`partial_hashing`, `full_hashing`, `storing`) that the backend never emits. Frontend `getPhaseLabel` handles non-existent phases.                            | Align type with actual backend emissions, or implement granular phase events in backend.                               |
| 19  | MEDIUM   | Correctness     | `src-tauri/src/scanner/detector.rs:194`               | `unique_files = total_files - duplicate_count` counts originals of duplicate groups as unique. E.g., 10 files, 2 groups of 3 → unique_files = 6 (should be 4).                                | Subtract ALL files in groups: `files_in_groups = groups.iter().map(g.files.len()).sum()`.                              |
| 20  | MEDIUM   | Correctness     | `src/lib/utils/deletionOrchestrator.ts:67-72`         | `updateResultsAfterDeletion` doesn't recalculate `unique_files`. After deleting duplicates, groups dropping to 1 file should increase unique count, but stale value is preserved.             | Recalculate unique_files based on eliminated groups.                                                                   |
| 21  | MEDIUM   | Correctness     | `src/lib/components/ResultsView.svelte:91-99`         | `handleSelectAllExceptOriginal` replaces entire selection with current group's non-originals. Cross-group Smart Selection results are silently discarded.                                     | Merge with existing selections instead of replacing.                                                                   |
| 22  | MEDIUM   | Docs/Plans      | `CLAUDE.md:53`                                        | Dead Code section says HashError re-export is 'needed once hash errors are surfaced to callers' — condition is met but re-export path is still unused (deletion.rs bypasses it).              | Fix deletion.rs to use the re-export path, then remove the suppression.                                                |
| 23  | MEDIUM   | Docs/Plans      | `docs/plans/05-results-ui.md (5.1.5)`                 | Plan specifies 'duplicate-found' live streaming event and `liveGroups` store. Neither implemented — current impl waits for complete `scan-results`.                                           | Implement live streaming or mark Phase 5.1.5 as deferred in plan index.                                                |
| 24  | MEDIUM   | Docs/Plans      | `docs/plans/07-scan-progress.md (7.2-7.6)`            | Plan specifies pause/resume commands, ScanProgressDisplay component, progress persistence. None implemented — only cancel exists.                                                             | Mark Phases 7.2-7.6 as not started in plan index.                                                                      |
| 25  | MEDIUM   | Error Handling  | `src-tauri/src/commands/scan.rs:37`                   | Five `let _ = self.handle.emit(...)` calls silently discard emit errors. Progress/error/completion events lost with no logging.                                                               | Replace with `if let Err(e) = ... { log::warn!(...) }` at least for critical events.                                   |
| 26  | MEDIUM   | Error Handling  | `src-tauri/src/commands/scan.rs:154`                  | If Mutex is poisoned during scan cleanup, `is_scanning` is never reset to false. App permanently blocks future scans.                                                                         | Use `AtomicBool` for `is_scanning` to avoid lock dependency in cleanup.                                                |
| 27  | MEDIUM   | Error Handling  | `src-tauri/src/services/scan.rs:158`                  | Failed session status update (`let _ = update_status(...)`) silently discarded. Session remains 'running' in DB permanently.                                                                  | Log the error from the status update.                                                                                  |
| 28  | MEDIUM   | Error Handling  | `src/lib/components/DeletionHistoryPanel.svelte:32`   | Bare `catch {}` silently swallows all errors. User sees '0 files / 0 B freed' with no indication of failure.                                                                                  | Log the error; consider showing a UI indicator.                                                                        |
| 29  | MEDIUM   | Error Handling  | `src/lib/stores/scanStore.svelte.ts:63`               | `init()` has no try/catch around five sequential `listen()` calls. Any failure → unhandled promise rejection, store stays uninitialized.                                                      | Wrap in try/catch, set `_error`, log failure.                                                                          |
| 30  | MEDIUM   | Maintainability | `src-tauri/src/scanner/walker.rs:125-384`             | Entry-processing logic (~80 lines) duplicated across `walk()`, `walk_with_callback()`, and `walk_channel()`.                                                                                  | Extract into `process_entry()` helper method.                                                                          |
| 31  | MEDIUM   | Maintainability | `src-tauri/src/db/queries.rs:196-271`                 | ScanSession row-to-struct mapping duplicated identically in `get_latest()` and `get_paused()`.                                                                                                | Extract `row_to_session()` helper.                                                                                     |
| 32  | MEDIUM   | Maintainability | `src-tauri/src/commands/scan.rs:235-300`              | God function: 65-line handler with DB row mapping, iteration, and aggregation. Domain logic belongs in service layer.                                                                         | Move reconstruction into `ScanService::get_results()`.                                                                 |
| 33  | MEDIUM   | Performance     | `src-tauri/src/scanner/detector.rs:206-219`           | `group_by_size` builds HashMap without pre-sizing. For millions of files, multiple rehashes.                                                                                                  | Use `HashMap::with_capacity(files.len() / 4)`.                                                                         |
| 34  | MEDIUM   | Performance     | `src-tauri/src/db/queries.rs:729-751`                 | `cleanup_missing_files` builds SQL IN clause with one placeholder per path. 100K+ files → exceeds SQLite max variable count.                                                                  | Process in batches of ~500, or use a temporary table.                                                                  |
| 35  | MEDIUM   | Performance     | `src/lib/components/DuplicateGroupsList.svelte:53`    | All groups rendered without virtualization. Thousands of groups → slow render, high memory, janky scroll.                                                                                     | Implement virtual scrolling.                                                                                           |
| 36  | MEDIUM   | Performance     | `src-tauri/src/services/scan.rs:94-102`               | Cancel flag polling every 50ms wastes cycles and adds cancellation latency.                                                                                                                   | Share a single `AtomicBool` directly instead of polling bridge.                                                        |
| 37  | MEDIUM   | Performance     | `src-tauri/src/scanner/hasher.rs:90-111`              | `full_hash` doesn't leverage BLAKE3's parallel hashing for large files. `full_hash_parallel` exists but is dead code.                                                                         | Use `full_hash_parallel` for files above 1MB threshold.                                                                |
| 38  | MEDIUM   | Security        | `src-tauri/src/commands/scan.rs:91`                   | No path validation on user-supplied scan paths. Arbitrary paths accepted without canonicalization or boundary check.                                                                          | Canonicalize with `std::fs::canonicalize()` before scanning.                                                           |
| 39  | MEDIUM   | Security        | `src-tauri/src/commands/deletion.rs:32-39`            | No validation that deletion paths were part of scan results. Compromised frontend could craft deletion requests for arbitrary files.                                                          | Validate paths against current scan session's `scanned_files` table.                                                   |
| 40  | MEDIUM   | Security        | `src-tauri/capabilities/default.json:6`               | `shell:allow-open` granted without scope restrictions. Allows opening arbitrary URLs/files via system shell.                                                                                  | Add scope restriction to limit URL schemes.                                                                            |
| 41  | MEDIUM   | Simplification  | `src-tauri/src/scanner/walker.rs:125-384`             | ~80 lines of entry-processing logic copy-pasted across three walk methods.                                                                                                                    | Extract `process_entry()` helper.                                                                                      |
| 42  | MEDIUM   | Simplification  | `src-tauri/src/db/queries.rs:196-271`                 | Identical row-to-struct closures in `get_latest()` and `get_paused()`.                                                                                                                        | Extract `row_to_session()` helper.                                                                                     |
| 43  | MEDIUM   | Simplification  | `src/lib/components/DuplicateGroupsList.svelte:13-36` | `getFileExtension`/`getFileTypeIcon` duplicate logic from `format.ts`.                                                                                                                        | Import from `$lib/utils/format`.                                                                                       |
| 44  | MEDIUM   | Simplification  | `src/lib/components/DeleteConfirmDialog.svelte:40-56` | Focus-trap logic duplicated across 3 dialog components.                                                                                                                                       | Extract shared `trapFocus` utility.                                                                                    |
| 45  | MEDIUM   | Types           | `src-tauri/src/db/models.rs:19`                       | `ScannedFile.size` is `i64` but file sizes are non-negative. Mismatch with `FileInfo.size` (u64) requires unchecked conversion.                                                               | Add checked cast or document the SQLite constraint.                                                                    |
| 46  | MEDIUM   | Types           | `src-tauri/src/db/models.rs:41`                       | `ScanSession.status` is bare `String` instead of `ScanStatus` enum. Any arbitrary string is representable.                                                                                    | Change to `ScanStatus` type or add accessor method.                                                                    |
| 47  | MEDIUM   | Types           | `src/lib/types.ts:128`                                | `ScanRequest.parallelism` typed as `string?` but backend expects only 3 values.                                                                                                               | Change to `'light' \| 'normal' \| 'aggressive'`.                                                                       |
| 48  | MEDIUM   | Types           | `src-tauri/src/services/deletion.rs:29`               | `DeletionResult` uses `bool success + Option<String> error` — invalid combinations representable (`success=true, error=Some`).                                                                | Replace with a Result-style enum.                                                                                      |
| 49  | MEDIUM   | Tests           | `src/lib/utils/deletionOrchestrator.ts`               | Zero test coverage for 5 exported functions with non-trivial logic critical to deletion correctness.                                                                                          | Create `tests/utils/deletionOrchestrator.test.ts`.                                                                     |
| 50  | MEDIUM   | Tests           | `src-tauri/src/scanner/hasher.rs`                     | No test for hashing nonexistent file path — primary error path untested.                                                                                                                      | Add `test_partial_hash_nonexistent_file` asserting `Err(HashError::Io(_))`.                                            |
| 51  | MEDIUM   | Tests           | `src-tauri/src/services/deletion.rs`                  | `trash_verified()` success path untested. Only empty-input test exists.                                                                                                                       | Add integration test with real temp files.                                                                             |
| 52  | MEDIUM   | Tests           | `src-tauri/src/scanner/detector.rs:206`               | `group_by_size()` symlink filtering never tested. No test passes `FileInfo` with `is_symlink=true`.                                                                                           | Add test with symlink entries verifying exclusion from groups.                                                         |
| 53  | MEDIUM   | Tests           | `tests/utils/format.test.ts:180-183`                  | `getFileExtension('/test/Makefile')` returns full path instead of empty string. Test documents the bug instead of testing correct behavior.                                                   | Fix `getFileExtension` to return `''` for extensionless files.                                                         |
| 54  | MEDIUM   | Tests           | `src-tauri/src/scanner/walker.rs:207`                 | Error-in-callback branch untested. No test exercises a callback returning `Err`.                                                                                                              | Add test with failing callback; verify `stats.errors` incremented.                                                     |
| 55  | MEDIUM   | Tests           | `src-tauri/src/scanner/tests.rs`                      | `follow_symlinks=true` never tested. If symlink-following has a bug (e.g., infinite loop), it wouldn't be caught.                                                                             | Add test with `follow_symlinks: true` and symlinked file.                                                              |
| 56  | LOW      | Architecture    | `src/lib/components/DuplicateGroupsList.svelte:13-36` | Utility functions duplicated from `$lib/utils/format.ts`.                                                                                                                                     | Import shared utilities.                                                                                               |
| 57  | LOW      | Architecture    | `src/lib/components/FileDetailsPanel.svelte:16-43`    | `getDirectory` duplicated from `$lib/utils/format.ts` with minor divergence.                                                                                                                  | Import from `$lib/utils/format`.                                                                                       |
| 58  | LOW      | Architecture    | `src-tauri/src/state.rs:18-19`                        | Scan state split between `AppState` and `ScanState` — must coordinate both manually.                                                                                                          | Consolidate scan fields into `ScanState`.                                                                              |
| 59  | LOW      | Architecture    | `src-tauri/src/commands/deletion.rs:169`              | DB model `DeletionRecord` leaked through command boundary. Other commands map to DTOs.                                                                                                        | Define response DTO or document as intentional exception.                                                              |
| 60  | LOW      | Architecture    | `src-tauri/src/commands/settings.rs:57`               | DB model `Setting` leaked through command boundary.                                                                                                                                           | Same as above.                                                                                                         |
| 61  | LOW      | Comments        | `src-tauri/src/state.rs:43`                           | Doc says 'Get a reference' but returns cloned `Arc`.                                                                                                                                          | Change to 'Get a cloned handle to the database'.                                                                       |
| 62  | LOW      | Comments        | `src-tauri/src/db/mod.rs:3-5`                         | Blanket `#![allow(dead_code)]` justified by `close()` method, but covers entire module.                                                                                                       | Apply `#[allow(dead_code)]` only on `close()`.                                                                         |
| 63  | LOW      | Comments        | `src/lib/components/FileDetailsPanel.svelte:16`       | Duplicated `getDirectory` with no comment explaining why.                                                                                                                                     | Import from shared utils or document divergence.                                                                       |
| 64  | LOW      | Comments        | `src/lib/components/DuplicateGroupsList.svelte:25-36` | Duplicated `getFileTypeIcon` with no comment explaining the different return type.                                                                                                            | Import and adapt shared utility.                                                                                       |
| 65  | LOW      | Docs/Plans      | `src/lib/types.ts:44-45`                              | TS `ScanProgress` has `started_at_ms`/`estimated_time_remaining_ms` fields not in Rust struct — never populated.                                                                              | Add to Rust struct or remove from TS until Phase 7.4.                                                                  |
| 66  | LOW      | Docs/Plans      | `docs/plans/05-results-ui.md (5.1.5)`                 | Plan code examples use Svelte 4 `writable/derived` but implementation uses Svelte 5 runes.                                                                                                    | Update plan examples.                                                                                                  |
| 67  | LOW      | Docs/Plans      | `docs/plans/06-selection-deletion.md (6.2)`           | Plan shows single-file `trash::delete()` but implementation uses `trash::delete_all()` (Issue #15).                                                                                           | Update plan to document two-phase approach.                                                                            |
| 68  | LOW      | Docs/Plans      | `docs/plans/07-scan-progress.md (7.1.2)`              | Plan specifies estimation with `EstimationState` and directory tracking. Only `estimated_total: None` exists.                                                                                 | Mark as deferred or implement.                                                                                         |
| 69  | LOW      | Docs/Plans      | `docs/plans/06-selection-deletion.md (6.3)`           | Plan doesn't document `group_ids` field added to `DeleteFilesRequest`.                                                                                                                        | Update plan with the field.                                                                                            |
| 70  | LOW      | Error Handling  | `src-tauri/src/commands/deletion.rs:81`               | `let _ = handle.emit(...)` discards progress event errors.                                                                                                                                    | Add `log::debug!` on emit failure.                                                                                     |
| 71  | LOW      | Error Handling  | `src/lib/components/FolderPicker.svelte:24`           | Catch block only logs to console. Native dialog failure → button appears to do nothing.                                                                                                       | Surface error to user via UI.                                                                                          |
| 72  | LOW      | Error Handling  | `src/App.svelte:76`                                   | Malformed saved JSON → only console log. User unaware paths were lost.                                                                                                                        | Show brief toast or set error state.                                                                                   |
| 73  | LOW      | Maintainability | `src-tauri/src/services/scan.rs:94-102`               | Polling loop to link cancel flags is fragile and wastes cycles.                                                                                                                               | Share single `AtomicBool` directly.                                                                                    |
| 74  | LOW      | Maintainability | `src/lib/components/DeleteConfirmDialog.svelte:40-56` | Focus-trap logic duplicated across 3 dialogs.                                                                                                                                                 | Extract reusable utility.                                                                                              |
| 75  | LOW      | Maintainability | `src-tauri/src/commands/settings.rs`                  | 4-line DB handle extraction pattern repeated 11+ times across commands.                                                                                                                       | Extract `get_db()` helper.                                                                                             |
| 76  | LOW      | Maintainability | `src-tauri/src/state.rs:17-19`                        | Scan state split across `AppState` and `ScanState`.                                                                                                                                           | Consolidate into single struct.                                                                                        |
| 77  | LOW      | Maintainability | `src-tauri/src/services/scan.rs:114`                  | Magic number: progress emitted every 100 files with no named constant.                                                                                                                        | Define `const PROGRESS_EMIT_INTERVAL: usize = 100;`.                                                                   |
| 78  | LOW      | Performance     | `src/lib/components/ResultsView.svelte:18`            | `sortedGroups` re-sorts on every reactive update. Backend already sorts by `wasted_space`.                                                                                                    | Remove redundant client-side sort.                                                                                     |
| 79  | LOW      | Performance     | `src-tauri/src/services/deletion.rs:91-123`           | `verify_batch` hashes files sequentially. Hundreds of large files → slow.                                                                                                                     | Use `rayon::par_iter` for concurrent verification.                                                                     |
| 80  | LOW      | Security        | `src-tauri/src/scanner/walker.rs:284-286`             | `follow_symlinks` could traverse into sensitive areas if enabled. Currently defaulted to false.                                                                                               | Ensure it can't be overridden from frontend, or add confirmation.                                                      |
| 81  | LOW      | Security        | `src-tauri/src/commands/settings.rs:32-51`            | `set_setting` accepts arbitrary key-value pairs with no key validation.                                                                                                                       | Validate against allowlist of known keys.                                                                              |
| 82  | LOW      | Security        | `src-tauri/src/commands/protected.rs:11-28`           | `add_protected_folder` doesn't validate path exists or canonicalize.                                                                                                                          | Canonicalize and verify directory exists.                                                                              |
| 83  | LOW      | Security        | `src/App.svelte:75-76`                                | `JSON.parse(value)` on settings DB data without schema validation.                                                                                                                            | Add runtime validation after parse.                                                                                    |
| 84  | LOW      | Simplification  | `src/lib/components/FileDetailsPanel.svelte:14-43`    | `getDirectory()` duplicated from `format.ts`.                                                                                                                                                 | Import shared function.                                                                                                |
| 85  | LOW      | Simplification  | `src-tauri/src/db/queries.rs:517-553`                 | ScannedFile row mapping duplicated in `get_by_group()` and `get_by_path()`.                                                                                                                   | Extract `row_to_scanned_file()` helper.                                                                                |
| 86  | LOW      | Simplification  | `src-tauri/src/services/scan.rs:86-102`               | Cancel flag polling task — unnecessary when walker could accept external flag.                                                                                                                | Pass external flag directly to walker.                                                                                 |
| 87  | LOW      | Simplification  | `src-tauri/src/commands/scan.rs:143-147`              | Two consecutive `app_handle.clone()` calls when one suffices.                                                                                                                                 | Reuse the original for one of the two uses.                                                                            |
| 88  | LOW      | Simplification  | `src-tauri/src/scanner/hasher.rs:51-87`               | `partial_hash` allocates fresh buffers instead of reusing `self.buffer`.                                                                                                                      | Reuse `self.buffer[..chunk_size]`.                                                                                     |
| 89  | LOW      | Simplification  | `src/lib/utils/deletionOrchestrator.ts:75-90`         | `computePendingDeletionSize` rebuilds file-size map already built in `buildDeletionRequests`.                                                                                                 | Compute size from the `DeletionRequest[]` array directly.                                                              |
| 90  | LOW      | Types           | `src/lib/types.ts:132`                                | `FileType` union includes `'all'` — a filter sentinel mixed with domain values.                                                                                                               | Use separate `FilterFileType = FileType \| 'all'`.                                                                     |
| 91  | LOW      | Types           | `src/lib/api/deletion.ts:16`                          | `getDeletionHistorySummary` returns `[number, number]` — positional tuple with no named fields.                                                                                               | Define `DeletionHistorySummary` interface.                                                                             |
| 92  | LOW      | Types           | `src-tauri/src/scanner/types.rs:62-78`                | Pre-epoch timestamps and missing timestamps both map to 0 — indistinguishable.                                                                                                                | Use `Option<i64>` for `created_at`/`modified_at`.                                                                      |
| 93  | LOW      | Types           | `src/lib/utils/format.ts:80`                          | `getFileTypeIcon` returns unconstrained string. Only 5 values valid.                                                                                                                          | Define `FileCategory` type for return value.                                                                           |
| 94  | LOW      | Types           | `src/lib/stores/scanStore.svelte.ts:11`               | `ScanPhase` transitions not validated — raw assignment allows invalid state machine transitions.                                                                                              | Model valid transitions explicitly.                                                                                    |
| 95  | LOW      | Types           | `src/lib/utils/deletionOrchestrator.ts:17`            | Non-null assertion `!` used on `fileMap.get()` after `.filter()` guard. Logically safe but silences type checker.                                                                             | Use `flatMap` with narrowing check instead.                                                                            |
| 96  | LOW      | Tests           | `tests/types.test.ts`                                 | 40 tests only validate TypeScript object construction — compile-time checks duplicating `svelte-check`.                                                                                       | Reduce to smoke test; replace with runtime behavior tests.                                                             |
| 97  | LOW      | Tests           | `src-tauri/src/scanner/hasher.rs:117`                 | `full_hash_parallel()` has no test verifying it produces same hash as `full_hash()`.                                                                                                          | Add equivalence test with large file.                                                                                  |
| 98  | LOW      | Tests           | `src-tauri/src/scanner/detector.rs:28`                | `DuplicateGroup::new()` edge cases (0, 1 files) not directly unit-tested.                                                                                                                     | Add direct constructor tests.                                                                                          |

## Review Process

| Dimension           | Status   | Notes                                                                                       |
| ------------------- | -------- | ------------------------------------------------------------------------------------------- |
| Correctness & Bugs  | Reviewed | Traced execution paths; found unique_files miscalculation and unsafe DB casts               |
| Tests & Coverage    | Reviewed | Ran test suite: 85 frontend + 68 backend passed, 0 failed. Found untested modules           |
| Error Handling      | Reviewed | Found silent failures in event emission, DB cleanup, and mutex poisoning                    |
| Types & Type Design | Reviewed | Found type mismatches between Rust/TS, boolean-with-payload anti-patterns                   |
| Comments & Docs     | Reviewed | Found 6 stale dead-code annotations, phase type mismatch between frontend/backend           |
| Simplification      | Reviewed | Found 3 major DRY violations (walker, queries, dialog focus-trap)                           |
| Security            | Reviewed | npm audit: 1 high + 3 moderate deps; cargo audit: not installed. Found path validation gaps |
| Performance         | Reviewed | Found single-threaded hashing, unbatched DB writes, missing virtual scrolling               |
| Maintainability     | Reviewed | Found split state management, repeated boilerplate, magic numbers                           |
| Architecture        | Reviewed | Found layer violations in scan results and deletion commands, race condition in cancel      |
| Code vs Docs/Plans  | Reviewed | Compared against 5 plan files. Found unimplemented features (pause/resume, live streaming)  |

## Limitations

- **cargo audit** is not installed — Rust dependency vulnerabilities could not be checked. Install with `cargo install cargo-audit` and run `cargo audit`.
- **Code coverage tooling** is not configured — quantitative coverage gaps could not be measured. Coverage findings are based on manual inspection.
