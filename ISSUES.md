# Codebase Issues Tracker

| #   | Issue                                                                                                                                      | Impact | Area     | Status                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------ | -------- | ----------------------------------------------------- |
| 1   | `App.svelte` is a ~710-line god-component managing scanning, deletion, history, and navigation all in one file                             | High   | Frontend | Fixed                                                 |
| 2   | `scanStore.ts` is defined and tested but never used — `App.svelte` reimplements all state inline                                           | High   | Frontend | Fixed                                                 |
| 3   | `formatBytes` duplicated in 3 places, `formatDate` in 2 places, `getFileName` in 2 places instead of importing from `$lib/utils/format.ts` | High   | Frontend | Fixed                                                 |
| 4   | Tracked files that should be gitignored: `dist/`, `.obsidian/`, `.DS_Store` (3 files), `src-tauri/.env`, `src-tauri/gen/`                  | Medium | Git      | Not an issue (files already gitignored and untracked) |
| 5   | No `ScanService` in Rust backend — scan orchestration (~200 lines) lives inside `commands/scan.rs`                                         | Medium | Backend  | Fixed                                                 |
| 6   | `formatters.test.ts` re-implements functions inline instead of importing from utility modules                                              | Medium | Tests    | Fixed                                                 |
| 7   | `src/lib/.gitkeep` is vestigial — directory has real content                                                                               | Medium | Frontend | Fixed                                                 |
| 8   | `src-tauri/src/db/schema.rs` defines table name constants that are never used (dead code)                                                  | Medium | Backend  | Fixed                                                 |
| 9   | Fragmented docs: `Specification.md` at root + `plans/` directory should be under `docs/`                                                   | Low    | Docs     | Fixed                                                 |
| 10  | Leftover `greet` command in production code                                                                                                | Low    | Backend  | Fixed                                                 |
| 11  | No typed API wrapper layer for `invoke()` calls from frontend                                                                              | Low    | Frontend | Open                                                  |
| 12  | Two overlapping test files: `formatters.test.ts` and `format.test.ts`                                                                      | Low    | Tests    | Fixed (resolved with #6)                              |
| 13  | `scanStore.ts` uses Svelte 4 `writable`/`derived` while `App.svelte` uses Svelte 5 runes — architectural inconsistency                     | Low    | Frontend | Fixed (resolved with #2)                              |
| 14  | Clear Selection button only accessible when Smart Select panel is open                                                                     | Medium | Frontend | Fixed                                                 |
